/** Node.js, на котором собирается и запускается сам CLI. */
export const CLI_NODE_ENGINE_RANGE = '^26.0.0';

/** Диапазон Node.js, который scaffold кладёт в engines сгенерированного приложения. */
export const NODE_ENGINE_RANGE = '^22.12.0 || ^24.11.1 || ^26.0.0';
